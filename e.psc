pile 1 = grade_card_data.json
A = 0 
while (Pile 1 has more cards) {
    Read the top card X from pile 1
    if ( X.total > 220 ) {
        n = A + 1
        B = 1 + 2
        b = alu + bhindi
    }
    move X to Pile 2
}

while (Pile 1 has more cards) {
    Read the top card X from pile 1
    if ( X.total > 220 ) {
        n = A + 1
        B = 1 + 2
        b = alu + bhindi
    }
    move X to Pile 2
}